{
  "name": "serverless-plugin-import-resources",
  "version": "0.1.1",
  "description": "Serverless plugin that allows to import resources into an existing stack",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "serverless"
  ],
  "author": "Kevin Giguere Lacasse",
  "license": "ISC",
  "peerDependencies": {
    "serverless": "^2.60 || 3"
  },
  "devDependencies": {
    "serverless": "^3.30.1"
  }
}
